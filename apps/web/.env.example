# Google OAuth 2.0 Web client id (Google Cloud Console → APIs & Services → Credentials).
# Used by the sign-in page to render the Google button. Public value (safe in the bundle);
# the backend verifies tokens against the matching Auth:Google:ClientId.
# Copy this file to `.env.local` and fill in your client id.
VITE_GOOGLE_CLIENT_ID=
